console.log(document);

/* A propriedade document, que vem do objeto window funciona como console, uma propriedade que também é 
    um objeto.
    Serve para ver os dados do documento, nesse caso o que tem nos arquivos e só temos até então o arquivo
    index.html, que na pratica só ia mostar isso ao inspecionar a pagina.
    Posso digitar somente document no console do navegador que vai funcionar da mesma forma*/